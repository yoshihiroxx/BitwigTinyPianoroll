import React from 'react';
import { AppBar } from '@material-ui/core';
import { styled } from '@material-ui/core/styles';

const PrefAppBar = styled(AppBar)({
  background: '#353535',
  color: 'white'
});

export default PrefAppBar;

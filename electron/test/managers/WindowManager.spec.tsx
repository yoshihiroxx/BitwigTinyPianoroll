import { spy } from 'sinon';
import WindowManager from '../../app/managers/WindowManager';

describe('managers', () => {
  it('should increment should create increment action', () => {
    const wm = new WindowManager();
  });
  // There's no nice way to test this at the moment...
});
